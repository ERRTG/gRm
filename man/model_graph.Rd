% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api-model-graph.R
\name{model_graph}
\alias{model_graph}
\alias{plot.gRm_model}
\title{Build or plot the score-inclusive GLLRM graph}
\usage{
model_graph(object, ...)

\method{plot}{gRm_model}(
  x,
  ...,
  layout = c("score_left", "fr", "kk", "nicely"),
  x_spacing = 2.5,
  y_spacing = 1.4,
  item_spacing = 1.6,
  vertex.label = NULL,
  vertex.color = NULL,
  vertex.shape = NULL,
  vertex.size = NULL,
  vertex.label.cex = NULL,
  vertex.label.dist = NULL,
  vertex.label.degree = NULL,
  edge.color = NULL,
  edge.lty = NULL,
  edge.width = NULL,
  margin = NULL,
  rescale = FALSE,
  xlim = NULL,
  ylim = NULL
)
}
\arguments{
\item{object}{A \code{gRm_model} or \code{gRm_fit} object.}

\item{...}{Reserved for S3 dispatch compatibility; ignored by
\code{model_graph()} methods.}

\item{x}{A \code{gRm_model} or \code{gRm_fit} object.}

\item{layout}{Graph layout used by \code{plot()}. \code{"score_left"} keeps the
source-faithful deterministic layout with \code{Score} on the left and uses a
gRm-specific layered renderer. \code{"fr"}, \code{"kk"}, and \code{"nicely"} use
automatic \code{igraph} layouts and the \code{igraph} renderer.}

\item{x_spacing}{Horizontal spacing multiplier for \code{layout = "score_left"}.}

\item{y_spacing}{Vertical spacing multiplier for \code{layout = "score_left"}.}

\item{item_spacing}{Additional vertical spacing multiplier for item nodes
when \code{layout = "score_left"}. Increase this when item nodes are visually
too close together.}

\item{vertex.label}{Optional vertex labels.}

\item{vertex.color}{Optional vertex colors.}

\item{vertex.shape}{Optional vertex shapes. For \code{layout = "score_left"},
\code{"circle"} and \code{"rectangle"} are supported.}

\item{vertex.size}{Optional vertex sizes.}

\item{vertex.label.cex}{Optional vertex-label scaling.}

\item{vertex.label.dist}{Optional vertex-label distance. For
\code{layout = "score_left"}, this scales the distance between the node marker
and its label.}

\item{vertex.label.degree}{Optional vertex-label angle passed to
\code{plot.igraph} for automatic \code{igraph} layouts.}

\item{edge.color}{Optional edge colors.}

\item{edge.lty}{Optional edge line types.}

\item{edge.width}{Optional edge widths.}

\item{margin}{Optional plot margin.}

\item{rescale}{Logical value passed to \code{plot.igraph}. The default is
\code{FALSE}. This is used only for automatic \code{igraph} layouts.}

\item{xlim}{Optional x-axis limits.}

\item{ylim}{Optional y-axis limits.}
}
\value{
An undirected \code{igraph} object with vertex attributes \code{label}, \code{type},
\code{order}, \code{x}, and \code{y}, and edge attributes \code{type}, \code{source}, \code{status},
\code{from_label}, and \code{to_label}.

\code{plot()} methods draw the graph and return the \code{igraph} object
invisibly.
}
\description{
\code{model_graph()} returns the score-inclusive IRT graph for a gRm model or
fitted model. The graph always contains the score node. Items are connected
to score by baseline score edges, LD terms add item-item edges, and DIF terms
add item-exogenous edges.
}
\details{
The official gRm graph is the IRT graph with score. The package deliberately
does not expose a score-free graph because removing score changes the graph's
interpretation. A Rasch model is therefore a star centered on \code{Score}; GLLRM
local-dependence and DIF terms are added to that baseline graph.

The default score-left plot is a layered rendering of the same graph. LD
edges between adjacent plotted item rows are drawn as straight item-item
links. LD edges between non-adjacent plotted item rows are curved away from
the item column so they are not mistaken for dependence among all intervening
items.
}
\examples{
data <- data.frame(
  ID = 1:8,
  I1 = c(0, 1, 0, 1, 0, 1, 1, 0),
  I2 = c(1, 0, 1, 0, 1, 0, 1, 0),
  site = c(0, 0, 1, 1, 0, 1, 0, 1)
)
analysis <- gRm(data, items = c("I1", "I2"), exogenous = "site", id = "ID")
model <- gllrm(analysis, ld = ~ I1:I2, dif = ~ I1:site)
graph <- model_graph(model)
}
