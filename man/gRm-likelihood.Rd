% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api-likelihood-comparison.R
\name{gRm-likelihood}
\alias{gRm-likelihood}
\alias{logLik.gRm_fit}
\alias{anova.gRm_fit}
\title{Likelihood methods for fitted gRm models}
\usage{
\method{logLik}{gRm_fit}(object, ...)
\method{anova}{gRm_fit}(object, ..., test = "Chisq")
}
\arguments{
\item{object}{A fitted model returned by \code{\link[=fit]{fit()}}.}

\item{...}{For \code{anova.gRm_fit()}, additional \code{gRm_fit} objects fitted to the
same gRm analysis. For \code{logLik.gRm_fit()}, reserved for S3 compatibility
and ignored.}

\item{test}{Test column to include in the model-comparison table. \code{"Chisq"}
and \code{"LRT"} request the standard likelihood-ratio chi-square p-value
computed with \code{\link[stats:Chisquare]{stats::pchisq()}}. Use \code{NULL}, \code{"none"}, or \code{FALSE} to omit
the p-value column.}
}
\value{
\code{logLik.gRm_fit()} returns a standard \code{"logLik"} object using R's
sign convention. gRm stores DIGRAM's source-style negative log likelihood
internally: if the fitted object stores \code{L = -log(likelihood)},
\code{logLik(fit)} returns \code{-L}.

\code{anova.gRm_fit()} returns an \code{"anova"} data frame with columns:
\code{"Model Df"}, \code{"-2 logLik"}, \code{"Df"}, \code{"Chisq"}, and, when requested,
\code{"Pr(>Chisq)"}.
}
\description{
Compare fitted gRm models with a likelihood-ratio table that follows the
shape of R model-comparison output while using likelihood-ratio terminology.
}
\details{
gRm stores the source-faithful DIGRAM quantity \code{-log(likelihood)} on fitted
objects. \code{logLik.gRm_fit()} converts that value to R's standard log
likelihood sign, matching the sign convention used by \code{\link[stats:logLik]{stats::logLik()}}.
\code{anova.gRm_fit()} reports \verb{-2 logLik} for each model and the
likelihood-ratio change between consecutive rows. Its printed format
follows the same style as \code{anova(glm_fit0, glm_fit1, test = "Chisq")},
but uses likelihood-ratio column names instead of residual-deviance names.
The test statistic remains the source-faithful likelihood-ratio statistic,
while the p-value is computed with R's standard
\code{stats::pchisq(..., lower.tail = FALSE)} when the row has a positive
degrees-of-freedom increase and non-negative likelihood-ratio statistic.
Reversed or otherwise invalid comparison rows keep the signed diagnostic
\code{Df} and \code{Chisq} values, but \verb{Pr(>Chisq)} is set to \code{NA}.

The comparison assumes that supplied models are nested and fitted to the same
gRm analysis. The method checks the available analysis identity and
likelihood sample-size metadata, but it does not prove mathematical
nestedness.
}
\examples{
data <- data.frame(
  ID = seq_len(12L),
  I1 = c(1L, 1L, 2L, 2L, 3L, 3L, 1L, 2L, 3L, 1L, 2L, 3L),
  I2 = c(1L, 2L, 1L, 3L, 2L, 3L, 3L, 1L, 2L, 2L, 3L, 1L),
  I3 = c(2L, 1L, 3L, 1L, 2L, 3L, 2L, 1L, 3L, 3L, 2L, 1L),
  site = c(1L, 1L, 1L, 2L, 2L, 2L, 1L, 1L, 2L, 2L, 1L, 2L)
)
analysis <- gRm(
  data,
  items = c("I1", "I2", "I3"),
  exogenous = "site",
  id = "ID",
  score_cuts = "auto"
)
fit0 <- fit(gllrm(analysis))
fit1 <- fit(gllrm(analysis, ld = ~ I1:I2))

# gRm stores DIGRAM's negative log likelihood internally, but logLik()
# returns the usual R signed log likelihood.
logLik(fit0)

# Compare nested fitted models with an anova-style likelihood-ratio table.
anova(fit0, fit1, test = "Chisq")
}
\seealso{
\code{\link[=fit]{fit()}}, \code{\link[=gllrm]{gllrm()}}
}
