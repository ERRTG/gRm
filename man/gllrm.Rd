% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api-model-spec.R
\name{gllrm}
\alias{gllrm}
\title{Specify a GLLRM model}
\usage{
gllrm(project, ld = NULL, dif = NULL)
}
\arguments{
\item{project}{A \code{gRm_analysis}, \code{gRm_project}, \code{gRm_screen}, or fitted object.}

\item{ld}{One-sided formula of local-dependence item:item terms.}

\item{dif}{One-sided formula of DIF item:exogeneous terms.}
}
\value{
A \code{gRm_model} object. This function specifies a model; it
does not fit it.
}
\description{
Specify a GLLRM model
}
\details{
A GLLRM model defines a score-inclusive IRT graph. Every item is connected
to the score node, LD terms add item:item edges, and DIF terms add
item:exogenous edges. Use \code{\link[=model_graph]{model_graph()}} to return that graph as an
\code{igraph} object, or \code{plot(model)} to draw it with the score node on the left.
}
\examples{
data <- data.frame(
  ID = 1:6,
  I1 = c(0, 1, 0, 1, 0, 1),
  I2 = c(1, 0, 1, 0, 1, 0),
  site = c(0, 0, 1, 1, 0, 1)
)
analysis <- gRm(data, items = c("I1", "I2"), exogenous = "site", id = "ID")
model <- gllrm(analysis, ld = ~ I1:I2, dif = ~ I1:site)
summary(model)
graph <- model_graph(model)
\donttest{
plot(model)
}
}
\seealso{
\code{\link[=model_graph]{model_graph()}}, \code{\link[=fit]{fit()}}, \code{\link[=update.gRm_model]{update.gRm_model()}}
}
