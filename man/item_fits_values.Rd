% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/item_fits_values.R
\name{item_fits_values}
\alias{item_fits_values}
\title{Compact DIGRAM ItemFits values}
\usage{
item_fits_values(
  project,
  max_step = 5000L,
  max_delta = 1e-04,
  include_extended = TRUE
)
}
\arguments{
\item{project}{A parsed DIGRAM project from \code{\link[=read_digram_project]{read_digram_project()}}.}

\item{max_step}{Maximum number of base Rasch IPF iterations.}

\item{max_delta}{Convergence threshold for the base Rasch fit.}

\item{include_extended}{Whether to compute the extended ItemFits diagnostic
surface. Compact workflows can set this to \code{FALSE} to avoid the
source-faithful but much larger extended table pass.}
}
\value{
A \code{gRm_item_fits_values} object with per-item outfit, infit,
item-restscore gamma, p-values, BH flags, compact side-file values, and BH
limits for the combined compact result family.
}
\description{
Computes source-shaped ItemFits numeric values from the native R Rasch fit.
The implementation is independent R production code: Pascal references are
used only by tests, not by this function.
}
\details{
The outfit/infit branch intentionally preserves a mathematically strange
DIGRAM source convention from \code{skbias15.pas}: observed item-score proportions
can be counted on valid-background rows while the score weights come from all
complete item-response rows. This mixed conditioning is required for
source-faithful numeric reconstruction.
}
\examples{
\dontrun{
project <- read_digram_project("path/to/DIGRAM")
values <- item_fits_values(project)
values$items
}
}
\keyword{internal}
