% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ari_plot.R
\name{plot.gRm_ari}
\alias{plot.gRm_ari}
\title{Plot DIGRAM ARI item score curves}
\usage{
\method{plot}{gRm_ari}(
  x,
  ...,
  class_size = 40L,
  rows = NULL,
  columns = NULL,
  items = NULL,
  confidence = 0.95,
  show_expected = FALSE
)
}
\arguments{
\item{x}{A \code{gRm_ari} table returned by \code{\link[=ari]{ari()}}. The table must contain the
item number, item name, total score, row count, observed mean, expected
mean, and expected variance columns produced by \code{\link[=ari]{ari()}}.}

\item{...}{Reserved for future extensions. Supplying any argument through
\code{...} is an error so misspelled plotting arguments are reported instead of
silently ignored.}

\item{class_size}{Positive whole number giving the minimum target number of
persons in each displayed score interval. Adjacent raw total scores are
collapsed until this target is reached. Defaults to \code{40}, matching the
reference DIGRAM ARI plotting macro.}

\item{rows}{Optional positive whole number giving the number of rows in the
faceted plot layout. If both \code{rows} and \code{columns} are supplied, their
product must be large enough for the selected items.}

\item{columns}{Optional positive whole number giving the number of columns in
the faceted plot layout. If both \code{rows} and \code{columns} are supplied, their
product must be large enough for the selected items.}

\item{items}{Optional item selector used to draw only a subset of items. Supply
either item numbers from the \code{ItemNo} column or item names from the \code{Item}
column. Numeric and character selectors cannot be mixed, and the source item
order is preserved after filtering.}

\item{confidence}{Number between 0 and 1 giving the confidence level for the
expected band. The critical value is calculated with
\code{stats::qnorm((1 + confidence) / 2)} for every confidence level.}

\item{show_expected}{Single logical value. If \code{TRUE}, draw the expected mean
as a dashed line in addition to the observed mean and expected confidence
band. The default \code{FALSE} mirrors the reference DIGRAM/SAS plot.}
}
\value{
A \code{ggplot} object.
}
\description{
Draw the DIGRAM ARI item mean curves for a \code{gRm_ari} table returned by
\code{\link[=ari]{ari()}}. The plot collapses raw total scores into adjacent class intervals,
then shows the observed item mean score and a model-expected confidence band
for each item.
}
\details{
The observed line is \code{sum(n * ObsMean) / sum(n)} within each item and score
interval. The expected band is centered at \code{sum(n * ExpMean) / sum(n)} and
uses the weighted expected variance \code{sum(n * ExpVar) / sum(n)}. Item names
are shown as ordinary ggplot2 facet-strip titles. No files are read or
written.
}
\examples{
data <- data.frame(
  ID = 1:8,
  I1 = c(0, 1, 0, 1, 0, 1, 1, 0),
  I2 = c(1, 0, 1, 0, 1, 0, 1, 0)
)
fitted <- fit(gllrm(gRm(
  data,
  items = c("I1", "I2"),
  id = "ID",
  score_cuts = c(1L, 2L)
)))
ari_table <- ari(fitted)
plot(ari_table)
}
