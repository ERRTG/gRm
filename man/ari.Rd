% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api-ari.R
\name{ari}
\alias{ari}
\title{DIGRAM ARI item score curves}
\usage{
ari(fit, ...)
}
\arguments{
\item{fit}{A fitted gRm model.}

\item{...}{Reserved for future extensions; ignored.}
}
\value{
A data frame with class \code{gRm_ari}. Rows follow DIGRAM's
item-major, score-minor order. The columns are \code{ItemNo}, \code{Item}, \code{Score},
\code{n}, \code{Obs0..ObsK}, \code{ObsMean}, \code{ObsVar}, \code{Exp0..ExpK}, \code{ExpMean},
\code{ExpVar}, and \code{z}, where \code{K} is the global maximum item score.
}
\description{
Compute the DIGRAM ARI item-by-total-score table for a fitted gRm model.
}
\details{
ARI is the item response distribution by raw total score produced by the
DIGRAM \verb{ITA 18} path. The returned observed and expected category columns are
probabilities, not raw counts; the raw row count is stored in \code{n}. Total
score \code{0} and the maximum possible total score are excluded, following the
source call to \code{Calculate_Ari} with \code{LeastScore = 1} and
\code{LargestScore = highest_possible_score - 1}.

Values are returned at native R precision. The function does not write
\code{Ari_dot.csv} or \code{Ari_comma.csv}.
}
\examples{
data <- data.frame(
  ID = 1:8,
  I1 = c(0, 1, 0, 1, 0, 1, 1, 0),
  I2 = c(1, 0, 1, 0, 1, 0, 1, 0)
)
fitted <- fit(gllrm(gRm(
  data,
  items = c("I1", "I2"),
  id = "ID",
  score_cuts = c(1L, 2L)
)))
ari_table <- ari(fitted)
print(ari_table)
head(ari_table)
plot(ari_table)
}
