% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api-model-spec.R
\name{update.gRm_model}
\alias{update.gRm_model}
\title{Update a DIGRAM model specification}
\usage{
\method{update}{gRm_model}(object, ld = NULL, dif = NULL, ...)
}
\arguments{
\item{object}{A \code{gRm_model} object created by \code{\link[=gllrm]{gllrm()}}.}

\item{ld}{Replacement local-dependence formula. Use \code{NULL} to keep the
current LD terms. Use \code{~ 0} to remove all LD terms.}

\item{dif}{Replacement DIF formula. Use \code{NULL} to keep the current DIF
terms. Use \code{~ 0} to remove all DIF terms.}

\item{...}{Reserved for S3 dispatch compatibility; ignored.}
}
\value{
An updated \code{gRm_model} object. The returned model is not fitted;
pass it to \code{\link[=fit]{fit()}} to estimate parameters.
}
\description{
Replace LD and/or DIF terms in an existing DIGRAM model specification.
}
\details{
\code{update.gRm_model()} keeps the original analysis object and replaces only
the requested model-term families. Formula syntax is the same as in
\code{\link[=gllrm]{gllrm()}}: LD terms are item:item interactions and DIF terms are
item:exogenous interactions.

If the original model came from \code{\link[=screen]{screen()}}, screen-selected terms are
retained only while their source remains present in the updated model terms.
}
\examples{
data <- data.frame(
  ID = 1:8,
  I1 = c(0, 1, 0, 1, 0, 1, 1, 0),
  I2 = c(1, 0, 1, 0, 1, 0, 1, 0),
  site = c(0, 0, 1, 1, 0, 1, 0, 1)
)
analysis <- gRm(
  data,
  items = c("I1", "I2"),
  exogenous = "site",
  id = "ID",
  score_cuts = c(1L, 2L)
)
model <- gllrm(analysis)
updated <- update(model, ld = ~ I1:I2, dif = ~ I1:site)
summary(updated)
}
\seealso{
\code{\link[=gllrm]{gllrm()}}, \code{\link[=fit]{fit()}}
}
