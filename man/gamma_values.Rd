% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gamma_values.R
\name{gamma_values}
\alias{gamma_values}
\title{Derive DIGRAM marginal gamma report values}
\usage{
gamma_values(project)
}
\arguments{
\item{project}{A parsed DIGRAM project from \code{\link[=read_digram_project]{read_digram_project()}}.}
}
\value{
A \code{gRm_gamma_values} object.
}
\description{
Computes the Goodman-Kruskal marginal association-gamma matrix printed by
DIGRAM's \code{GAMMA} command. The source path is
\code{DGRexe.execute_marginal_gamma}, which builds one two-variable marginal
hypothesis for every ordinal pair and stores \code{results[1,5]}. That value is
produced by \code{SkStat.RCGAMMA}; the same quadrant-count formula is mirrored in
\code{SourceRaschCore.SourceRCGammaStats}.
}
\details{
Production R computes from \code{DIGRAM.var} and \code{DIGRAM.dat}; Pascal and the
supplied DIGRAM report are test oracles only.
}
\examples{
\dontrun{
project <- read_digram_project("path/to/DIGRAM")
values <- gamma_values(project)
values$gamma[1:3, 1:3]
}
}
\keyword{internal}
