% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/screen_values.R
\name{screen_values}
\alias{screen_values}
\title{Derive DIGRAM graphical SCREEN report values}
\usage{
screen_values(project, significance = 0.05)
}
\arguments{
\item{project}{A parsed DIGRAM project from \code{\link[=read_digram_project]{read_digram_project()}}.}

\item{significance}{SCREEN significance level. DIGRAM defaults to \code{0.05}.}
}
\value{
A \code{gRm_screen_values} object.
}
\description{
Computes the source-shaped graphical \code{SCREEN} matrices from a parsed DIGRAM
project. This is the graphical model screening command implemented by
\code{DIGRAM1f.Execute_Screening} and \code{SKscr4.ISCREEN}; it is distinct from the
item-screening \verb{SCREEN J} report.
}
\details{
The implemented slice covers the two-way table stage, hidden-association
stage, final model-3 revision stage, incomplete-path warnings, and the
current-model partial gamma matrix from the same source branch. Partial gamma
is computed after the SCREEN acceptance path copies \code{MODEL_3} back to the
project graph, through a native R port of \code{DGRexe.execute_gamma(false)},
\code{Generate_hypotheses}, \code{CheckHyp}, and \code{quick_tests}; substituting marginal
gamma or an earlier SCREEN stage here would be source-unfaithful.

Production R computes from \code{DIGRAM.var} and \code{DIGRAM.dat}; Pascal and the
supplied DIGRAM report are test oracles only.
}
\examples{
\dontrun{
project <- read_digram_project("path/to/DIGRAM")
values <- screen_values(project)
values$final_model$status_chars[1:3, 1:3]
}
}
\keyword{internal}
