% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/global_homogeneity_values.R
\name{global_homogeneity_values}
\alias{global_homogeneity_values}
\title{Derive DIGRAM global homogeneity numeric values}
\usage{
global_homogeneity_values(
  project,
  score_cuts,
  max_step = 5000L,
  max_delta = 1e-04,
  bundle = NULL,
  base_fit = NULL,
  fit = NULL
)
}
\arguments{
\item{project}{A parsed DIGRAM project from \code{\link[=read_digram_project]{read_digram_project()}}.}

\item{score_cuts}{Integer upper score cuts. For the supplied validation runtime
example this is \code{c(30, 87)}, corresponding to \verb{CUT 30 87}.}

\item{max_step}{Maximum number of Rasch IPF iterations.}

\item{max_delta}{Convergence threshold for Rasch IPF.}
}
\value{
A \code{gRm_global_homogeneity_values} object with the full-model fit,
score-group fits, item mean rows, and CLR summary.
}
\description{
Computes non-GUI DIGRAM global homogeneity values from raw DIGRAM
input using native R code.
}
\details{
The item mean rows are computed from the shared \code{skbias15.pas}-shaped helper
used by both global homogeneity and global invariance. Row counts, observed
means, expected means, and summary CLR/df/p values are protected by tests.
The printed item-mean residual and marker cells are deliberately \code{NA}
because the available source does not fully explain the historical runtime's
hidden residual variance materialization.
}
\examples{
\dontrun{
project <- read_digram_project("path/to/DIGRAM")
values <- global_homogeneity_values(project, score_cuts = c(30, 87))
values$summary
}
}
\keyword{internal}
