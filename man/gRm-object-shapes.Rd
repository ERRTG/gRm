% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gRm-package.R
\name{gRm-object-shapes}
\alias{gRm-object-shapes}
\title{gRm object shapes}
\description{
Main \code{gRm} functions exchange S3 lists with compact public classes such
as \code{gRm_analysis}, \code{gRm_model}, \code{gRm_fit}, and result classes for
each diagnostic family. This topic documents the expected internal
structures for package contributors and tests.
}
\section{Project object}{

Returned by \code{\link[=read_digram_project]{read_digram_project()}}. Contains \code{paths}, \code{variables}, \code{items},
\code{backgrounds}, and \code{raw_data}. \code{variables}, \code{items}, and \code{backgrounds} are
data frames with source label code, raw data position, raw maximum category,
variable name, and item/background flag.
}

\section{Bundle object}{

Returned by \code{\link[=build_item_parameters_bundle]{build_item_parameters_bundle()}}. Contains \code{model}, \code{manifest},
and \code{data}. \code{model} stores item/background metadata and maximum total score.
\code{manifest} stores source-style read and validity counts. \code{data} stores
recoded zero-based item scores, background values, total score, status, and
missing/invalid flags.
}

\section{Fit object}{

Returned by \code{\link[=fit_rasch_base]{fit_rasch_base()}}. Contains iteration diagnostics, convergence
state, fitted item gamma parameters, expected item counts, update ratios, and
observed count summaries.
}

\section{Values object}{

Returned by numeric value helpers. Contains item labels, gamma values,
thresholds, locations, ICE/MICE effects, item statistics, observed score
range, estimated parameter count, and convergence diagnostics for public
accessors.
}

\keyword{internal}
